= form_for([project, project.tasks.build]) do |f|
  .task-input
    = f.text_field :name, size: 50, placeholder: "Enter at least 10 symbols, max 255", required: "required"
  = f.submit "Create task", class: "btn btn-success"
